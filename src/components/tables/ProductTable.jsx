import { useQuery,useMutation } from "@tanstack/react-query";


const ProductTable = () => {

 //this hook is for reading the product data   
 const {} = useQuery({

 })

 //this hook is for updating the product data
 const {} = useMutation({

 })

  return (
    <div>ProductTable</div>
  )
}

export default ProductTable;